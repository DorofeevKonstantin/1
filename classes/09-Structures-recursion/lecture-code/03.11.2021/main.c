#include <stdio.h>

#include "utils.h"

int main()
{
	// modul in proggramming == interface(opened part) + realization(closed part)
	// modul in c == utils.h + utils.c
	
	// call function
	hello_world(6);
	return 0;
}