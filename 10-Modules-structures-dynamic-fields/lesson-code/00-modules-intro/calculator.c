#include "calculator.h"

int intSum(int first, int second) { return first + second; }
int intSub(int first, int second) { return first - second; }
int intMult(int first, int second) { return first * second; }
int intDiv(int first, int second) { return first / second; }
